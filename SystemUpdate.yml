---
- name: Sytem Update
  hosts: all
  sudo: yes
  tasks:
    - name: Update Cache
      yum: update_cache=yes
      when: ansible_os_family == "RedHat"
      
    - name: Install Updates
      yum: name=* state=latest
      when: ansible_os_family == "RedHat"
  
